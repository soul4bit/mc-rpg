{
  "name": "mc-rpg-launcher-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "React + Electron shell for MC RPG launcher",
  "main": "electron/main.cjs",
  "scripts": {
    "backend:package": "mvn -q -DskipTests package",
    "dev": "node scripts/dev-runner.mjs",
    "vite": "vite",
    "build": "npm run backend:package && vite build",
    "start": "npm run build && node scripts/run-electron.mjs",
    "prepare:dist": "node scripts/prepare-dist.mjs",
    "dist": "npm run build && npm run prepare:dist && node scripts/package-portable.mjs",
    "dist:dir": "npm run build && npm run prepare:dist && node scripts/package-portable.mjs --dir"
  },
  "dependencies": {
    "react": "19.2.5",
    "react-dom": "19.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.1",
    "electron": "42.0.0",
    "vite": "8.0.10"
  }
}
